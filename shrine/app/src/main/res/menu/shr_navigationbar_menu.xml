<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/info"
        android:icon="@drawable/shr_info"
        android:title="@string/shr_general_information_title"
        android:checked="false"
        app:showAsAction="ifRoom" />
    <item
        android:id="@+id/home"
        android:icon="@drawable/shr_home"
        android:title="@string/shr_mainlist_title"
        android:checked="true"
        app:showAsAction="always" />
    <item
        android:id="@+id/favorite"
        android:icon="@drawable/shr_favorite"
        android:title="@string/shr_favorite_title"
        android:checked="false"
        app:showAsAction="ifRoom" />
</menu>
